server:
  port: 8091

spring:
  application:
    name: author-service
  datasource:
    url: jdbc:mysql://localhost:3306/ebookstore1?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: zyqzyq041230
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


