server:
  port: 8092

spring:
  application:
    name: price-function-service
  cloud:
    function:
      web:
        path: /

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info

