server.port=8080
#????MySQL??? ??JDBC????
spring.datasource.url=jdbc:mysql://localhost:3306/ebookstore?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=zyqzyq041230
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#?????????
#????SQL??
#???SQL
#??SQL??
spring.jpa.hibernate.ddl-auto=update    
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#????
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN

# Redis settings
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.timeout=2000
spring.redis.lettuce.pool.max-active=8
spring.redis.lettuce.pool.max-idle=8
spring.redis.lettuce.pool.min-idle=1

# Cache TTL (seconds)
app.cache.book.ttl-seconds=600

# App logging levels for cache demonstration
logging.level.org.springframework.data.redis=INFO
logging.level.com.ebookstore=INFO

# Console log pattern with timestamp, thread, logger and message
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} %5p --- [%t] %c{1.} : %m%n
spring.output.ansi.enabled=ALWAYS


server.servlet.session.cookie.same-site=lax
server.servlet.session.cookie.secure=false
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true

#??????
#???HTTP??
#???????
#??????
#???????????
spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
spring.web.cors.max-age=3600 